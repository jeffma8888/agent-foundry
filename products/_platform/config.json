{
  "name": "_platform",
  "repo": "{FOUNDRY}",
  "allowed_push_repo": "agent-foundry",
  "branch": "main",
  "vision": "{FOUNDRY}/VISION.md",
  "roadmap": "{FOUNDRY}/PLATFORM_ROADMAP.md",
  "quality_ref": "{FOUNDRY}",
  "test_cmd": "uv run --with pytest pytest -q",
  "roles_dir": "{FOUNDRY}/roles",
  "work_root": "{FOUNDRY}/products/_platform",
  "quality_bar": "This product IS the foundry framework itself. Improvements must keep foundry.py and dispatcher.py importable (python -c import), keep tests/ green, and preserve the design invariants documented in ARCHITECTURE.md (output-file success, anti-delegation, pessimistic gate, tester isolation, resilience, single-brain). Small, safe, reversible increments only. Never edit a currently-running loop's semantics in a way that breaks resume.",
  "push_enabled": true,
  "dual_pm_scouts": true
}
